import React from "react"
import { Container, Typography } from "@mui/material"

const ExpenseItem = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>รายการบันทึกค่าใช้จ่าย</Typography>
    </Container>
  )
}

export default ExpenseItem
