import React from "react"
import { Container, Typography } from "@mui/material"

const CreditNote = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>ใบลดหนี้ & ส่งคืนสินค้า</Typography>
    </Container>
  )
}

export default CreditNote
