import React from "react"
import { Container, Typography } from "@mui/material"

const ExpenseMaster = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>แฟ้มข้อมูลค่าใช้จ่ายอื่น ๆ</Typography>
    </Container>
  )
}

export default ExpenseMaster
