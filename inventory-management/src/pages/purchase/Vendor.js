import React from "react"
import { Container, Typography } from "@mui/material"

const Vendor = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>แฟ้มข้อมูลผู้ขาย & เจ้าหนี้</Typography>
    </Container>
  )
}

export default Vendor
