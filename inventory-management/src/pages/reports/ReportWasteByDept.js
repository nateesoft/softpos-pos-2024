import React from "react"
import { Container, Typography } from "@mui/material"

const ReportWasteByDept = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>สรุปรายการบันทึกของเสียตามแผนกสินค้า (Dept)</Typography>
    </Container>
  )
}

export default ReportWasteByDept
