import React from "react"
import { Container, Typography } from "@mui/material"

const ReportTranout = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>แสดงรายการใบรับโอนสินค้า (ออก)</Typography>
    </Container>
  )
}

export default ReportTranout
