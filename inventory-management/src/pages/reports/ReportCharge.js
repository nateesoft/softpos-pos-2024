import React from "react"
import { Container, Typography } from "@mui/material"

const ReportCharge = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>แสดงรายการบันทึกรายการแจกฟรี (Charge)</Typography>
    </Container>
  )
}

export default ReportCharge
