import React from "react"
import { Container, Typography } from "@mui/material"

const ReportChargeByDept = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>สรุปรายการแจกฟรี (Charge) ตามแผนกสินค้า (Dept)</Typography>
    </Container>
  )
}

export default ReportChargeByDept
