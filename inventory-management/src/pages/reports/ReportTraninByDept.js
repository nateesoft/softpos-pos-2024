import React from "react"
import { Container, Typography } from "@mui/material"

const ReportTraninByDept = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>สรุปรายการรับโอนสินค้า (เข้า) ตามแผนกสินค้า (Dept)</Typography>
    </Container>
  )
}

export default ReportTraninByDept
