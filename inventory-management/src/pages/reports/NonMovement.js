import React from "react"
import { Container, Typography } from "@mui/material"

const NonMovement = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>รายงานสินค้าที่ไม่เคลื่อนไหว (Non Movement)</Typography>
    </Container>
  )
}

export default NonMovement
