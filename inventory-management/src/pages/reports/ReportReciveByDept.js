import React from "react"
import { Container, Typography } from "@mui/material"

const ReportReciveByDept = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>สรุปรายการรับสินค้าจากโรงงานตามแผนกสินค้า (Dept)</Typography>
    </Container>
  )
}

export default ReportReciveByDept
