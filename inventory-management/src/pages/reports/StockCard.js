import React from "react"
import { Container, Typography } from "@mui/material"

const StockCard = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>บัญชีคุมสินค้าพิเศษ (Stock Card)</Typography>
    </Container>
  )
}

export default StockCard
