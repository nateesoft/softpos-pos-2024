import React from "react"
import { Container, Typography } from "@mui/material"

const ReportRecive = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>แสดงรายการใบรับสินค้าจากโรงงาน</Typography>
    </Container>
  )
}

export default ReportRecive
