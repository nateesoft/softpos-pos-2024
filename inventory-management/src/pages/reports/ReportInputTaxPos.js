import React from "react"
import { Container, Typography } from "@mui/material"

const ReportInputTaxPos = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>รายงานภาษีขาย (จาก POS)</Typography>
    </Container>
  )
}

export default ReportInputTaxPos
