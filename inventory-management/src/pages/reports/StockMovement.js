import React from "react"
import { Container, Typography } from "@mui/material"

const StockMovement = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>รายงานความเคลื่อนไหว (Stock Movement)</Typography>
    </Container>
  )
}

export default StockMovement
