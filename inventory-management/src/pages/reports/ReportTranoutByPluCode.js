import React from "react"
import { Container, Typography } from "@mui/material"

const ReportTranoutByPluCode = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>สรุปรายการรับโอนสินค้า (ออก) ตามรหัสสินค้า (PLU Code)</Typography>
    </Container>
  )
}

export default ReportTranoutByPluCode
