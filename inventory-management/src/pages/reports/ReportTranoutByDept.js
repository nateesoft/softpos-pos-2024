import React from "react"
import { Container, Typography } from "@mui/material"

const ReportTranoutByDept = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>สรุปรายการรับโอนสินค้า (ออก) ตามแผนกสินค้า (Dept)</Typography>
    </Container>
  )
}

export default ReportTranoutByDept
