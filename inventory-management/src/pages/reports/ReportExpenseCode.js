import React from "react"
import { Container, Typography } from "@mui/material"

const ReportExpenseCode = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>สรุปรายการค่าใช้จ่าย (ตามรหัสค่าใช้จ่าย)</Typography>
    </Container>
  )
}

export default ReportExpenseCode
