import React from "react"
import { Container, Typography } from "@mui/material"

const ReportInputTax = () => {
  return (
    <Container maxWidth="lg" sx={{ marginTop: 10 }}>
      <Typography>รายงานภาษีซื้อ</Typography>
    </Container>
  )
}

export default ReportInputTax
